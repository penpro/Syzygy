# Threat model

**Baseline:** 2026-07-14. This is a living Phase 0 inventory, not a completed security claim.

## Assets

Local research, prompts/responses, Drive refresh token, selected workspace content, downloaded
models, collaborative state, participant identity, updater signing trust, and future immutable
versions/evaluation evidence.

## Trust boundaries

1. React webview ↔ typed Tauri commands.
2. Rust core ↔ local filesystem.
3. Webview ↔ loopback llama.cpp.
4. Rust core ↔ Google OAuth/Drive over TLS.
5. Updater ↔ GitHub signed release metadata.
6. Local IndexedDB provider ↔ Yjs/domain state.
7. Configured collaboration provider (Drive live; WebSocket spine not yet product-bound) ↔ local Yjs/domain state.
8. Local MCP process ↔ authenticated loopback bridge ↔ live webview state.
9. Rust provider adapters ↔ remote model APIs and OS credential storage.
10. Future WASI/native-MCP plugin runtimes ↔ permission broker ↔ project/Drive/model services.

## Current threats and controls

| Threat | Current control | Residual risk / next test |
|---|---|---|
| Webview reads arbitrary local path | Canonical `Granted` allowlist | Junction/symlink race tests needed |
| Webview obtains OAuth token | Token/access exchange remains Rust-only; command removed | Audit every new Drive wrapper |
| Broad Drive token reads outside workspace | Descendant enumeration and selected-folder config | Application boundary can regress; add hostile-ID tests |
| Legacy scope yields empty evidence and model hallucinates | Collaboration access gate; Shared Ask fails closed | Live re-link harness outstanding |
| Malicious Drive text prompt-injects model | Text is labeled evidence; model has no ambient commands; custom function output is proposal-only and never executed | Live-provider injection corpus and any future authority-specific executor contract remain required |
| A remote model proposes a forged, oversized, malformed, or dangerous tool call | Custom function definitions and returned calls are bounded at the Rust request, provider decoder, runtime accumulator, and frontend reducer; IDs/names/fragments/final JSON must agree; incomplete or malformed calls fail closed. Unsafe schema keywords/references/patterns/branching fail before transmission. Rust and the pinned frontend validator independently classify complete arguments against the exact approved safe-subset schema; domain stays unreviewed, execution stays false, and the product has no result loop or MCP/Drive/filesystem/plugin/editor/network/shared-mutation authority | Structural schema success does not establish safe meaning or permission; any future executor must add tool-specific domain validation, explicit per-authority consent, result provenance, injection fixtures, live-provider proof, and a separate threat-model review |
| Malicious policy/heuristic/example text prompt-injects the checker or fabricates evidence | All snapshots are labeled untrusted content; adapters expose no command/tool/Drive authority; strict exact-key output is route-owned; every cited quote and UTF-16 span is checked against the frozen policy; one repair is the ceiling; stale revisions save nothing | A model can still produce persuasive but wrong rationale or select misleading true spans; live-provider injection corpus, semantic benchmarks, researcher override, and human review remain required |
| Concurrent collaborators make a model result appear current after its inputs change | Commit rechecks project/document identity, exact heuristic edit revision, exact active-example events, and exact semantic policy text before one immutable peer-namespaced append | Offline changes not yet delivered cannot be predicted; later results remain historical evidence and UI needs stronger outdated-result comparison/filtering |
| Prompt-injection-shaped policy/scenario text commands a rerun evaluator or fabricates an execution envelope | Exact sources are labeled untrusted; adapters expose no tools/Drive/ambient authority; only four semantic JSON fields are accepted and route/job/item/attempt/run/model provenance is application-owned | Models can still produce persuasive but wrong policy application; live-provider adversarial corpora, baseline comparison, and human review remain required |
| Crash, timeout, pause, or replay duplicates scenario-evaluation spend/results | Immutable begin persists before provider work; one item runs at a time; interrupted attempts retain the same run; result and complete commit atomically; exact replay is idempotent; completed items are never runnable; each item has a two-minute deadline and 30-second heartbeat | A provider may bill work that was cancelled or whose response was lost before atomic commit; cross-install crash-at-durability and provider idempotency evidence remain open |
| Concurrent or hostile queue state hides work, forks control, or substitutes a version/route/result | Strict bounded peer-namespaced definitions/events require unique item/scenario/run/result IDs, creator-only exact parents, linear chains, verified policy/scenario identities, and matching result provenance; forks/collisions/foreign items fail closed and surface in MCP inspection | Creator identity/time are installation supplied, recovery UI for corrupt queues is limited, and offline changes not yet delivered cannot be predicted |

| Oversized/nested Drive workspace exhausts resources | 2,000-file and 12-level direct-read bounds | Measure bytes/API calls and add attachment limits |
| Transcript leaks a local conversation | Shared toggle is explicit and UI copy names logging | Per-message inclusion controls are future work |
| Mirror conflict loses edits | Current LWW mirror documented as optional | Never use LWW for CRDT state; Phase 4 convergence tests |
| Corrupt or future local project state loads as trusted content | Manifest parser rejects malformed/unknown schema versions; migrations are idempotent | Fuzz Yjs payloads, archive bombs, unknown node types, and interrupted IndexedDB writes |
| A peer or plugin rewrites an immutable policy checkpoint | Versions are canonical bounded strings keyed by SHA-256; every read reparses, re-canonicalizes, and rehashes, and returns detached data | A malicious peer can make a hash entry unavailable by replacing it; future signed/exported archives and transport recovery must detect and repair denial-of-service without accepting mutation |
| A collaborator's display-name change rewrites historical attribution | Each version hashes both stable participant ID and display-name-at-save into its immutable envelope | Participant identity is caller-supplied and not yet authenticated across installs; Phase 3 identity enrollment remains open |
| Restore rewrites history or a stale client silently replaces a newer head | Restore copies a verified target into a new hash whose parent is the exact expected head; expected head and parent bytes are rechecked before the single Yjs transaction writes version plus head | Concurrent valid commits create branches and one deterministic visible head; product UI must expose/reconcile non-head branches instead of hiding them |
| AI-generated change notes conceal or hallucinate edits | The baseline diff and count note compare verified structured blocks locally with no engine/network dependency | Current non-policy blocks lack stable IDs, so changed prose can appear as remove/add; richer semantic notes must remain optional and cite the deterministic diff |
| Local provider is mistaken for real-time collaboration | UI says local persistence; Drive/presence controls remain disabled | Two-editor and two-install provider-contract gates must pass before collaboration claims |
| Path traversal through Drive filename | Direct reads are in memory; mirror joins remote names | Sanitize/reject separators before mirror writes |
| OAuth token stolen from app data | OS user boundary; no webview exposure | OS credential vault/encryption evaluation needed |
| Malicious update | Tauri updater signature and separate Syzygy key | Protect/back up signing key; clean-machine update tests |
| Diagnostics leak research/token | Typed invoke logger records command/error only | Automated canary/redaction tests needed |
| LAN or website pilots the live app | Ephemeral IPv4 loopback bind, random 256-bit bearer, browser-origin rejection, bounded HTTP parser | Verify OS firewall/listener state and hostile browser preflight on each platform |
| MCP overwrites a collaborator's newer draft | Every document write requires the exact revision from a prior live read | Revision is editor-state optimistic concurrency, not yet an attributed review/approval workflow |
| MCP integrity inspection leaks large research bodies or gains mutation authority | `inspect_research_state` returns at most 200 metadata summaries per collection, omits policy/scenario/guidance/edit/note and scenario-evaluation response/rationale/uncertainty bodies, routes to a read-only validator, and tests use secret canaries | Titles, scenario status/lineage, participant display names, IDs, and timestamps remain visible metadata to the already-connected local MCP host; response-size and timing budgets need packaged large-project proof |
| MCP inspects a stale or wrong collaboration document | Provider connect/disconnect registers by project and document identity; old strict-mode cleanup cannot remove a newer registration; inspection rechecks metadata project ID | Abrupt webview/provider failure may leave process-local state until teardown; packaged navigation/reopen churn proof remains open |
| MCP checkpoints a stale draft or attaches it to the wrong history head | Save requires the exact live document revision and expected immutable head; editor revision is checked before hashing and again inside the final transaction that also rechecks head/parent bytes | Lexical revision is process-session scoped and the participant identity is caller-supplied; packaged concurrent-edit proof and authenticated cross-install identity remain open |
| MCP creates a scenario from stale research state or in the wrong project | Inspection returns a monotonic Yjs state-vector revision; creation rechecks exact revision and live metadata project ID before one synchronous domain transaction; stale harness asserts zero writes | Participant identity/time are unauthenticated, and future asynchronous/multi-step mutations need transaction-local revision rechecks |
| MCP broad inspection leaks scenario bodies or a targeted read silently expands scope | `inspect_research_state` continues to omit every scenario background/turn/revision body. `read_scenario` separately discloses one background plus at most 1,000 turn identities/head pointers while omitting turn bodies; `read_scenario_turn_revision` requires exact scenario/turn and one optional immutable edit ID or index and returns one bounded body. Both validate the full graph and have no write/model/Drive path | A connected MCP host explicitly authorized to call either tool receives that selected research content; same-user compromise, access policy, cumulative repeated reads, and near-limit response timing remain outside this boundary |
| A stale product or MCP edit silently overwrites a collaborator's scenario turn | Product edits capture and recheck the exact selected head; stale drafts remain visible with Save disabled and add nothing. MCP add/revise also consume the exact monotonic research revision. Every accepted edit appends immutable attribution. Disconnected exact-parent siblings remain retained, expose the complete tip set, block ordinary editing, and reconcile only through an exact-head/all-tip merge revision from UI or MCP. | Head selection, participant identity, and time remain self/caller supplied; a researcher can choose misleading merge content. Authenticated identity, trusted clock/authorization policy, near-limit performance, packaged interaction, and physical two-install reconciliation proof remain open. |
| MCP double-counts, silently overwrites, or fabricates a scenario vote | Vote calls consume the exact monotonic research revision; stale requests fail before an event is added; immutable re-vote/withdrawal history projects one current choice per participant ID and MCP returns aggregates only | Participant ID/display name and process time are unauthenticated, so Sybil resistance, trusted identity/clock policy, moderation, and visible reconciliation remain open |
| MCP overwrites, leaks, or falsely attributes a collaborator's flag/note lifecycle | Create/edit/resolve/reopen consume exact research revision; non-create calls also require the exact current annotation event; immutable parent-linked history retains competing actions; mutation/inspection responses omit bodies and display names. Product and MCP best-effort attest the exact retained event only through an unconflicted registered installation for its participant claim and return explicit unsigned state after failures. | Caller still transmits bodies through the connected local MCP host; self-issued keys do not authenticate participant/time, and moderation, branch-reconciliation UI, trusted clock, and packaged large-body/two-install proof remain open |
| MCP silently overwrites a collaborator's context label or assignment | Create/rename/assign/remove consume the exact research revision; rename and follow-up assignment also require their exact current event; immutable history retains competing branches and stale calls add nothing | Label names remain visible metadata; participant/time are unauthenticated, and moderation, branch-reconciliation UI, trusted clock, and packaged large-label-set proof remain open |
| Stale MCP descriptor targets the wrong process | Descriptor includes schema/PID/version; connection and per-process token fail closed; normal shutdown removes it | Abrupt termination leaves a harmless stale descriptor until the next GUI launch; add PID liveness cleanup |
| Same-user malware steals the MCP token | User-local temp ACL (and `0600` on Unix); token rotates every GUI process | Not a same-user sandbox; evaluate OS named pipes/peer credentials before exposing higher-risk tools |
| Generated MCP instructions point to the wrong binary | UI and `syzygy_installation` share Rust `current_exe` discovery; JSON/TOML path-with-spaces tests run against the real binary | Reinstall/move can invalidate configuration; the guide tells users to regenerate it from the running app |
| MCP starts an adversarial job with stale/substituted research or gains ambient model authority | Start requires the exact live document revision and selected block indexes; webview derives sources; only built-in routes and the frozen graph are accepted; native disclosure precedes vault/network; result is non-mutating | Connected local MCP host still chooses the question, blocks, routes, and can read the completed research output; same-user host trust, packaged disclosure UX, per-project retention controls, and live-provider evidence remain open |
| Product or MCP silently shares, overwrites, tampers with, or auto-applies an adversarial review | Completed output remains transient until explicit save/share; product copy names the full content and Drive consequence. Save requires exact project/research revision and attribution, canonical-hashes the full archive, and replays strict request/plan/source/run/call/provider-provenance validation; peer-namespaced same-run conflicts remain visibly unresolved. Decisions require exact revision/archive hash/current parent, retain concurrent branches, and never touch editor root. Routine MCP inspection omits question/source/result/note bodies | Explicit share deliberately makes sensitive research available to the configured collaboration transport, including Drive; participant identity/time are unauthenticated; per-project retention/deletion controls, near-32-MiB decode/memory and usability proof, packaged two-install proof, and an independently guarded proposal/apply workflow remain open |
| Contract scaffolding is mistaken for a working feature | MCP exposes distinct truthful states for adapter conformance, ordinary native tasks, content-bound adversarial authorization/execution, collaborative archive/decision history, and contract-only plugins/custom adapters. The product workflow is separately compiled, headlessly rendered, and mechanically gated. | Adversarial execution/history still lack packaged-dialog interaction, live-provider, physical two-install product, authenticated-identity, large-archive usability, and quality evidence; availability claims must name the evidence tier |
| Remote review sends a stale or hidden draft or silently edits shared work | UI snapshots the current semantic editor revision, derives a SHA-256 source identity, and passes only typed question/source plus optional custom-function-schema fields; Rust derives disclosure; text and tool proposals remain transient non-mutating artifacts | Snapshot content is not persisted for later independent audit; concurrent edits after snapshot do not alter the in-flight call, and acceptance/rejection/provenance-export UI remains open |
| Fake-network record falsely names a production destination | Provider-run schema has an explicit `loopback-conformance` marker; semantic validation requires literal loopback in that mode and HTTPS otherwise; Rust→TypeScript harness validates the actual serialized record | Marker establishes test environment only, not live compatibility or provider policy behavior |
| New network code or stale privacy copy silently introduces an undisclosed destination | A strict versioned S-06 manifest anchors each active feature to activation/default state, destinations, payload/credential classes, source, and matching product copy; the headless scanner fails every unclassified production URL origin and emits origin-only proof with credential/query redaction canaries | Source literals do not prove runtime packet behavior, dynamic custom/LAN targets, SDK internals, DNS/CDN resolution, or paid-provider behavior; cross-platform OS packet capture and live opt-in canaries remain required before `verified` |
| A hostile/misconfigured self-hosted relay or leaked invitation reads, changes, loses, or replays collaborative state | Explicit acknowledgement and strict store-v7/v1/v2/v3/v4 parsing reject public plaintext, endpoint credentials/query/fragment/path, weak rooms, partial/extra access fields, and oversize input. Archives remove the whole live binding. The bundled relay retains its private/bounded/durable/reaped baseline. Managed rooms add digest-only random capabilities, one active-admin invariant, bounded optional expiry, and protocol-aware viewer enforcement at both client and relay. V4 binds a member to an operator-enrolled Ed25519 installation key, requires a fresh typed proof, and atomically consumes its nonce before protected data. Only a device-bound admin may open the reserved administration path; one strict action is independently signed over its semantic hash, room, member, exact revision, issue time, and the same nonce. Mutations durably reload membership and evict room peers. An optional host policy stores 1-16 exact signer bindings and a threshold; protected remote mutations then require unique valid project/room/revision/action-bound approvals from that set, while host-local emergency administration and rooms without policy remain available. Repeatable exact-process harnesses prove missing/wrong/revoked/expired/rotated-old/stale/replayed callers receive no protected frame, viewer local state cannot write during reconnect, five clients merge 60 rapid plus partitioned edits, remote issue/rotate/revoke retain no plaintext capability, a surviving second admin can rotate a lost admin member onto a replacement key without export/escrow, and eight adversarial shared-approval classes plus stale replay fail closed. Unregistered rooms remain explicitly legacy read/edit bearer mode. | V1-v3 capabilities remain copyable bearer secrets in webview/local project state and invitations; v4 resists invitation-only theft but does not authenticate a human. A compromised authorized admin installation can change room membership unless the host enabled a policy; configured quorum still proves installation keys rather than people and the host can bypass it locally. The relay host observes every action and frame. Expiry/freshness trust its clock; rotation does not deliver the replacement. Recovery requires a surviving admin or the relay host and cannot help after both are lost. Viewer local edits remain an unsynced local fork by design. Registry/update logs are same-user readable (capabilities are hashed, research updates/public enrollments are not encrypted), sync-log durability is not backup, and public TLS, hostile-frame fuzzing, physical packaged clients, trusted-time proof, authenticated human consent, and third-party relay authorization remain unproved. |
| A peer forges, mutates, replays, floods, or overstates collaboration identity | Each installation creates an Ed25519 key whose PKCS#8 bytes persist only in the OS credential store. Rust exposes public metadata plus exact presence, project-registration, named research-event, relay-access, relay-administrator, post-mutation relay-decision, and pre-mutation relay-approval signing commands, not arbitrary signing or key export. Owned encoded/decoded secret buffers zeroize on drop. Separate domain-separated canonical bytes bind presence, durable registration, one of ten named research-event kinds with exact event hash/identity/participant/time/nonce, exact room/member/capability/generation/time/nonce access, one semantic administrator action plus exact revision, one capability-free project/room/action/result statement, or one exact project/room/revision/action approval with bounded expiry. Research-event publication additionally requires the key to be an unconflicted project registration for that participant and the referenced live event hash to match. Strict schema/base64url/size checks and independent WebCrypto verification reject mutations. Relay access and administration share the one-minute freshness window and bounded atomic replay cache, while the server separately verifies currently enrolled admin role and any configured approval quorum. UI consistently says device/key and denies human verification. | Keys are self-issued, so a hostile peer can create a new key and claim another participant ID; local approval or durable registration does not grant access. Exact captured presence replay in the same context and durable registration, decision, or event-attestation replay remain valid. Product and MCP scenario votes, annotation lifecycle events, and label lifecycle/assignment events publish durable event attestations; seven other named domains remain caller-attributed. Relay enrollment and host policy are explicit operator decisions but do not prove who controls an installation; an enrolled admin has real room-membership authority when no policy is configured, and configured signers have real remote-mutation approval authority. No trusted clock, organizational binding, identity-key rotation/recovery, broad signed-event adoption, or physical two-install proof exists. This is device authorization and continuity, not authenticated human identity. |
| Local device approval is mistaken for shared identity or access control, or its registry is corrupted/rolled back | Only independently verified remote fingerprints receive approve/revoke controls. Native state is scoped to installation + project + key ID, rejects unknown fields/noncanonical keys/stale transitions, caps 1 MiB/64 projects/64 keys per project, serializes mutations, syncs a temporary file, and uses primary-first crash recovery. UI says **approved locally**/**revoked locally** and explicitly denies relay-access effects. | The app-data file is editable by the same OS user and stores current state, not signed history. Local clock values are informational. A missing primary may recover an older previous copy; key rotation appears unapproved; re-approval overwrites revoked state. Decisions do not propagate, bind a human, establish a role, revoke invitations, disconnect peers, or block relay/Yjs/MCP operations. |
| Durable device registration is mistaken for enrollment, leaks correlation unexpectedly, or is poisoned by a bearer peer | Publication is an explicit button with stable-fingerprint/cross-project correlation copy. Exact project + self-reported participant bytes are signed by the OS-vault key under a separate domain. Storage keys bind every signed identity field. Reads strictly parse/recompute/verify, cap 200 registration records and 1,000 settings entries, verify in batches of eight, preserve same-key participant conflicts, fail closed before writes when unhealthy, and omit invalid bodies from MCP. | The stable public key intentionally correlates registrations across projects. Registration is deterministic and replayable, not a nonce/history/access token. Any peer with Yjs write access can add self-issued keys, delete valid records, or flood within transport limits; the directory has no server authority, tombstone recovery, append-only guarantee, rate limit, or organizational enrollment. Local approval remains installation-only. |
| A shared signed relay decision is mistaken for a relay receipt, access policy, or complete audit log | Only a successful remote mutation result with the exact project, room, expected/resulting revision, affected member, and credential/report consistency can enter the builder. The installation signs a separate action hash and random record nonce; publication requires the same key to be an unconflicted verified project-directory entry. Strict records omit capabilities, cap 500 decisions/1,500 settings, verify eight at a time, preserve contradictory same-revision statements, and block extension of unhealthy history. UI names the relay as authority and the record as an installation statement. | The relay did not sign or acknowledge the shared record. A compromised admin can sign a false post hoc statement, omit a successful action, or use a misleading local clock. Host-local actions are absent. Yjs writers can replay, delete, withhold, or flood records and directory entries. The ledger neither grants/revokes relay roles nor proves a human, completeness, ordering across rooms, or server truth. |
| A shared relay approval is mistaken for authenticated consent or stronger enforcement than configured | A separate native Ed25519 domain binds one registered installation to the exact project, room, action hash, registry revision, seven-day-maximum expiry, and nonce. Strict Yjs inspection caps 500 records/2,000 settings, verifies eight at a time, counts a signer once, excludes expiry, and excludes same-revision signer equivocation. The host can install/remove an exact sorted signer set and threshold under a registry revision. The relay verifies the complete bundle against its own current policy and clock before a remote mutation; missing, duplicate, foreign, stale, mutated, expired, future, or invalid-signature bundles fail closed. Shared research-state inspection explicitly says host policy is absent from project state. Separate host-only MCP inspection requires the active project's exact local relay endpoint/room and omits member IDs, capabilities, public keys, storage paths, and research bodies; configuration requires the exact registry revision, eligible unconflicted key IDs, and an exact returned transition. | Policy is optional, the relay host remains an emergency local authority, and rooms without it permit an enrolled administrator to mutate independently. A same-user MCP client attached to the host can exercise that explicit operator surface. Project registration is self-issued, relay time is trusted, and Yjs writers can delete, withhold, replay, or flood approvals; deleting/withholding prevents progress rather than bypassing a configured relay quorum. No person/organization consent, withdrawal protocol, relay-signed receipt, policy attestation in shared state, public-WSS proof, or physical packaged-client proof exists. |
| Remote provider key leaks into webview/project/log/MCP | Masked uncontrolled input, immediate field clearing, no React/store persistence, redacted zeroizing Rust secret, OS-vault tests/live Windows canary, no key-return command, and runtime fixture scanning serialized output for the secret | A JavaScript string and DOM value exist transiently during entry; add DOM/heap/log/export/crash canaries and macOS/Linux live-vault proof before release |
| Provider caller forges, downgrades, or bypasses human disclosure | Public request has no approval/category/detached-source-ID fields; Rust derives categories and unique provenance from the same structured payload it serializes, then always opens a native dialog; denial test proves no vault read or network; transport still requires matching one-use approval | Product orchestration still chooses which frozen snapshots enter the envelope; bind that selection to the editor/domain revision and exercise the real packaged OS dialog before availability |
| Remote provider silently stores research or transmits to an unsafe endpoint | OpenAI, Gemini, and xAI one-shot/stream fixtures force `store:false`; every implemented remote request requires matching content disclosure and accepts only HTTPS or literal loopback HTTP; optional custom-function schemas are included in the disclosure | Built-in/server-side provider tools plus custom adapters need separate wire inspection; every provider policy must be rechecked at release |
| Anthropic adapter sends the wrong role/header shape or retains thinking content | Fake `/v1/messages` servers check `x-api-key`, pinned API version, system/user separation, bounded `input_schema` definitions, one-shot `stream:false`, SSE `stream:true`, event-stream media type, bounded response/stream bytes, text and partial-JSON proposal retention/schema status, cumulative usage, terminal ordering, sanitized failure, timeout, cancellation, and product-runtime secret/research/private-thinking omission | Tool execution/result continuation, domain validation/authority design, signed-thinking continuation, beta headers, upstream request IDs beyond the message ID, packaged-dialog interaction, and live policy evidence remain open |
| Gemini adapter stores research by default, drifts to preview, or retains thought output | Stable `/v1/interactions` fake servers check `x-goog-api-key`, `store:false`, `background:false`, bounded function definitions, one-shot `stream:false`, SSE `stream:true` plus event-stream accept/media type, `thinking_summaries:none`, text/proposal retention/schema status, bounded unique step indexes, interaction identity, usage consistency, terminal ordering, sanitized failure, timeout, cancellation, product routing, and secret/research/thought omission; `/v1beta` is rejected | Tool execution/result continuation, domain validation/authority design, thought-signature continuation, structured output, packaged-dialog interaction, and live policy evidence remain open |
| xAI Responses compatibility hides different retention semantics | Fake `/v1/responses` servers check one-shot/SSE `store:false`, no thread/cache identifier, bearer auth, bounded custom-function definitions/whole-call proposals/schema status, event-stream media type, bounded controls, and a mandatory boolean `x-zero-data-retention` header validated before event dispatch and preserved in the typed result/content-free record | Standard API traffic may still have 30-day retention when the header is false; WebSocket continuity, tool execution/result continuation, domain validation/authority design, encrypted reasoning, packaged dialog interaction, and live evidence remain open |
| Fragmented or future provider stream corrupts research evidence | Incremental OpenAI, Anthropic, Gemini, and xAI decoder tests cover fragmented frames and tool arguments, multiline data, future-event warnings, usage-before-finish, sanitized errors, private-body omission, provider identity, indexed-step identity/lifecycle, exact tool metadata/final-JSON agreement, and malformed/mismatched/oversized/truncated/incomplete failure; fake HTTP streaming proves media type, chunk dispatch, terminal order, aggregate bounds, distinct sanitized provider failure, timeout, cancellation, ordered scoped IPC delivery, bounded UI accumulation, and transient-only rendering | Slow-consumer/backpressure stress, retry/duplicate semantics, JSON-Schema/domain validation before any future execution, adversarial long-stream fuzzing, and live packaged calls remain open |
| Remote provider retains or trains on unexpected data | Provider profile separates state/training/ZDR and task disclosure is required | Policies change; re-check primary terms at adapter release and record policy date |
| Multi-model panel creates false confidence | Blind proposals, evidence audit, reversed judge order, minority retention, compute-matched baseline, strict result validation, and pending human review are executed | Protocol execution does not prove correctness or superiority; a public policy-research corpus, held-out scoring, statistics, and human review remain required |
| Adversarial run record hides unfavorable evidence or spends more compute than its baseline | Strict schema/semantic gates plus a content-bound native graph require complete audits, both judge orders, supported-minority disposition, exact compute-matched baseline calls, finite accounting, and no automatic mutation | Provider honesty, lost responses, billing after cancellation, corpus representativeness, and semantic correctness remain unproved |
| Plugin manifest grants itself authority | Manifests are requests; broker grants and rechecks permissions | Runtime broker not implemented; hostile permission suite required |
| Plugin package escapes its folder or fakes certification with only happy-path fixtures | Certifier resolves real paths, rejects traversal/symlink escape and unknown fields, requires valid+invalid proposal fixtures and at least one denied-authority probe | Runtime artifact parsing/signature, archive extraction, TOCTOU, install lifecycle, and actual denied-operation tests remain open |
| Native plugin escapes product controls | Native MCP is labeled an advanced unsandboxed process; WASI is preferred | Same-user native code retains OS authority; hashes/signatures do not make it a sandbox |
| Plugin overwrites newer or unreviewed work | Plugin mutations use bounded revision-guarded proposals plus human acceptance | Drive-specific proposal schemas and runtime acceptance UI remain open |
| Drive compaction drops an offline/concurrent update or leaves an unrecoverable partial state | The live provider pulls/flushes first and supplies only update IDs it has applied. Native code appends a complete content-addressed snapshot before moving any candidate; unknown concurrent IDs, failed moves, and IDs beyond the 200-record batch remain active. Moved records go to a recoverable same-project folder rather than Trash. Each native Drive-project HTTP request has a 30-second deadline and the whole post-snapshot validation/archive phase has a 60-second deadline with explicit safe-retry copy. MCP additionally rechecks exact document and research revisions after the final pull and before encoding/upload. Headless provider tests reconstruct on a clean installation, inject a concurrent record, and retry a partial move. | Real Drive interruption at every request boundary, quota/rate-limit behavior, archive retention/deletion policy, a repair UI, malicious same-user webview compromise, and physical two-install execution remain open |
| A large or slow Drive project catalog outlives MCP, multiplies requests, or leaves stale refresh work running | Selected and cross-workspace catalogs share a 12-second whole-operation timeout whose cancellation drops the pending future. Project manifest/title reads preserve listing order and run at most eight at a time inside one root; roots remain serial under the existing 200-root/1,000-project bounds. A compile-time assertion keeps that deadline below the 15-second authenticated live bridge response budget, and MCP derives a 20-second socket budget from the bridge constant. Rust proves ready success, timeout error sanitization, and pending-work drop; the structural audit locks both command wrappers and the timeout ordering. | Real Drive quota/rate-limit behavior, large-account latency distribution, partial-result/pagination UX, and a physical MCP timeout run remain unproved. A 12-second deadline can require retrying a legitimate slow catalog rather than returning partial results. |
| Concurrent/stale shared-project rename, interrupted retention, or malformed active history hides titles or mutates the wrong Drive project | The manifest is immutable. Renames append strict content-addressed project/document-bound events whose filenames are rehashed from description metadata and whose complete parents must exist. Product drafts keep the exact guards captured at first edit; native and MCP compare the complete sorted tip set after a final pull. Simultaneous writers remain visible siblings and reconciliation appends one all-tip child without deleting history. Retention snapshots the complete graph before recoverable archival. Repair separately hashes the exact active/archive/quarantine inventory, reconstructs only the maximal parent-closed graph, appends/reuses its canonical snapshot, re-lists every pre-existing record, and moves nothing on any unexpected change. Invalid active records move to recoverable quarantine, valid active records return to the archive, invalid archived/quarantined records remain untouched, and no path deletes or trusts quarantined data; a later-valid parent-complete quarantine record can contribute only through a new validated snapshot. MCP repair starts a four-slot background job with 30-second heartbeats so the 15-second bridge does not own a long mutation. | Display name/time are self-reported, a reconciler can choose misleading text, more than 20 recoverable tips requires manual adjudication, 5,000-event/4-MiB/501-active/5,200-each archive-and-quarantine/32-MiB-per-location limits still require policy, and real-Drive quota/interruption/process-loss/partial-move plus physical simultaneous-writer repair evidence remain open |
| A policy-block move deletes nested CRDT identity and loses or misattributes a concurrent edit | Canonical character content, bounded inline embeds/marks, schema, and review state live in a deterministic policy-ID Y.Text outside root placement; the bridge reprojects after collaboration/root transactions with Yjs writeback skipped; partition/reconnect and both separate append-only delivery orders converge on the correct moved ID; duplicate/malformed/legacy states fail reorder closed | Existing Drive projects with no single stable baseline are not auto-migrated and remain visibly gated; packaged two-install Drive interaction, large-document latency, hostile embed coverage, delete-vs-edit semantics, and coordinated legacy migration remain open |
| A stale or hostile scenario-response edit silently overwrites another researcher, hides model provenance, or exhausts the product view | Product creation/editing uses the same strict peer-namespaced revision model as generation; every human edit names the exact current parent and retains author/display-name snapshots, while model revisions retain provider/model/run. Stale and malformed histories write nothing and block generation before credential/provider work; the UI preserves the stale draft, requires reload, pages 50 responses, and renders at most 50 recent lineage revisions. Disconnected siblings remain inspectable and converge deterministically. | Deterministic current projection still uses caller-supplied time/revision ID; identity/time are unauthenticated, explicit sibling reconciliation is absent, and near-limit memory/latency plus packaged two-install Drive behavior remain unproven |
| Concurrent scenario work silently overwrites turns, drops a sibling, or creates corrupt revision history | Scenarios use nested field/turn CRDTs and an explicitly validated revision DAG; peer-specific internal keys retain colliding public IDs so reads fail closed. Each turn persists a selected head, projection derives the complete tip set, and ordinary revision stops when siblings exist. UI/MCP reconciliation requires the exact research revision, selected head, and complete tip set, then appends one revision whose parents are every sibling. Stale/incomplete requests are zero-write and late siblings reopen conflict. Strict deterministic v1 migration runs after IndexedDB/initial Drive pull and before automation, and archive import migrates before fingerprinting. | Head choice and participant/time remain caller/self supplied rather than authenticated; a valid user can deliberately choose misleading merge content. Packaged physical two-install reconciliation, move/reorder, near-limit latency, and recovery usability remain open. |
| Concurrent or replayed scenario votes double-count participants or overwrite dissent | Immutable events live in peer-specific versioned buckets; exact replay deduplicates; per-participant current projection retains every re-vote/withdrawal; conflicting event identities and orphan targets fail closed | Participant ID/display name/time are caller-supplied; authentication, trusted clock policy, moderation, recovery UI, and remote-provider proof remain open |
| Concurrent flag/note actions erase another researcher's edit or resolution | Immutable events name an exact parent; product operations require the current event; concurrent children remain in history and deterministic projection is delivery-order independent; collisions/orphan targets fail closed | Projection winner still uses caller time/event ID; authenticated identity/clock policy, branch reconciliation UI, moderation, and remote-provider proof remain open |
| Concurrent label rename or assignment hides another researcher's context | Immutable rename/add/remove events name the exact current parent; disconnected assignments merge; concurrent rename branches remain in history and deterministic projection is delivery-order independent; collisions/orphans fail closed | Projection winner uses caller time/event ID; authenticated identity/clock policy, moderation, recovery UI, and remote-provider proof remain open |
| A collaborator signs another author's research event or a changed policy checkpoint | Every adopted event resolver returns both the recomputed retained-event hash and retained author participant; verification requires both to equal the signed claim and requires an unconflicted registered installation for that participant. Scenario-turn revisions bind role/body/parents/source, policy save/restore binds the exact content-addressed envelope, and mutation of any retained field invalidates the attestation. | Participant IDs remain self-asserted, installation registration is not person/organization identity, five event domains are not yet adopted, and a malicious Yjs writer can delete or withhold otherwise valid history. |

| A baseline comparison hides changed inputs, calls a result better/worse, or exports tampered evidence | Comparison requires two distinct completed jobs with the same project/document/prompt/scenario IDs and byte-exact scenario revisions; policy, scenario-revision, and whole-artifact SHA-256 values plus every redundant identity/route/result join, executed-model set, timestamp, outcome-change flag, and nine-cell matrix are recomputed; UI reports neutral transitions only | Model hash, sampler, and seed were not captured by P30 and are explicit nulls with a nondeterminism warning; semantic correctness, causal improvement, authenticated identity, and live-provider reproducibility are not claimed |
| Comparison export leaks research through routine MCP inspection | Export is a deliberate native Save action with copy naming the policy/scenario/result bodies; `inspect_research_state` returns only the number of compatible completed pairs and existing content-free queue counts | Researchers can still intentionally save sensitive content to an unsafe destination; OS file permissions, redaction tooling, and packaged destination-policy enforcement remain open |
| A hostile or stale scenario pack injects authority, corrupts lineage, overwrites collaborator work, or hides tampering | File bytes, exact fields, supported schema version, IDs/text/history bounds, canonical ordering, persisted heads, complete parent DAG, SHA-256, and closed scenario ancestry are checked before import. New output is v2; a checksummed v1 pack is normalized into deterministic `migration-v1` chains only after verifying its original envelope. Exact duplicates are idempotent and any same-ID/different-content collision aborts the complete preplanned transaction; the format carries no executable/network/model/credential fields. | Author IDs and timestamps are self-asserted; a valid pack can contain deceptive research text or sensitive authoring history; OS destination/source trust, malware scanning, authenticated signatures, and third-party interop remain open. |

## Release blockers

- A path traversal or selected-workspace escape is high severity.
- Returning Drive tokens to the webview is high severity.
- Any AI action that mutates shared state without human acceptance is high severity.
- Any MCP addition that bypasses semantic domain/editor contracts or grants ambient Drive/filesystem/model authority is high severity.
- Any remote adapter that exposes a key to the webview or transmits before disclosure is high severity.
- Any plugin path that directly mutates project/Drive state or gains undeclared network/model authority is high severity.
- The plugin authority broker is an in-process decision layer, not a sandbox or operation host. It
  copies bounded snapshots, requires explicit grant subsets, returns pending proposals, and marks
  network/model/Drive decisions for downstream recheck. A future network host must resolve and
  reject local/private/link-local destinations and revalidate every redirect after the broker's
  hostname decision; a future loader must not treat a session ID as an OS security boundary.
- The adversarial runner keeps provider/model routing outside judge-visible artifacts and never
  mutates shared state. The native executor must continue binding exact research bytes and the full
  graph before disclosure, consuming calls atomically, verifying dependency hashes, deriving
  prompts in Rust, restricting endpoints to built-ins, and recording content-free provenance.
  Failures are intentionally consumed; retries require a new authorization and may incur duplicate
  provider billing.
- MCP adversarial jobs derive sources from the exact live revision, are bounded to eight active
  jobs, heartbeat every 30 seconds, abort at 15 minutes, and retain terminal results for one hour.
  This is explicit narrow model authority for the connected same-user host, not a sandbox or a
  general provider/Drive/filesystem/project-mutation capability. Packaged dialog and live-provider
  proof remain release evidence gaps.
- A WIT file is not a sandbox by itself. The published plugin baseline has no imports and bounded
  typed input/output validators, which prevents the contract from naming filesystem, network,
  environment, clock, randomness, Drive, model, or mutation authority. The baseline host now
  explicitly rejects top-level imports, uses an empty Wasmtime linker with no WASI dependency,
  enforces component/envelope/linear-memory/fuel/epoch ceilings, and revalidates output. Every run
  occurs in a fresh hidden child process; the parent bounds pipes, suppresses stderr, kills and reaps
  at five seconds, and proves recovery after a hostile Windows fuel-exhaustion worker abort. The
  status is `zero-import-subprocess-runtime-bounded`. This does not authorize package installation,
  trust publisher claims, compose authority-broker decisions, or make capability-bearing worlds
  safe. Every future host interface and every proposal acceptance still requires its own target,
  revision, disclosure, and human-review gate.
- Collaborative heuristics treat peer CRDT data as untrusted. Reads accept only the current schema,
  bounded stable IDs/text, known priorities, booleans, finite timestamps, and a complete valid edit
  map. Within a replica, update IDs are one-use: identical replay is idempotent and conflicting
  replay fails before mutation. Peer-specific internal storage keys keep independently created
  colliding edit IDs from overwriting one another; the validated projection detects the duplicate
  and fails closed after merge. Histories above 10,000 events are also rejected. The convergence
  harness fixes delete-versus-concurrent-edit semantics as deletion with no resurrection. This does
  not authorize a heuristic to run a model or mutate policy text.
- Claiming S-01 verified before the live Drive→local-model harness passes is a documentation defect.
