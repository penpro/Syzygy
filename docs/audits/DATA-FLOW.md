# Data-flow inventory

**Baseline:** 2026-07-14

| Flow | Trigger | Source → destination | Data | Persistence | Guard |
|---|---|---|---|---|---|
| Shared relay administration approval | A registered installation attempts one exact protected remote membership mutation; the relay host separately installs/removes signer policy | Exact semantic action/revision + project device key -> typed native approval signature -> strict shared Yjs record -> verified active bundle -> signed remote-admin request -> relay policy verification -> one registry transition | Project/room/revision/action hash/time/expiry/nonce plus public installation key/signature; no private key or member capability. Relay room report exposes threshold, configuration time, and key IDs, not signer public keys | Shared project Yjs/IndexedDB and configured transport for approvals; exact signer bindings/quorum in host-local `members-v1.json`; no separate MCP copy | 500 approval/2,000 setting/eight-verification shared bounds; policy has 1-16 unique exact keys; relay counts each configured signer once and checks its own current revision/time; mutation does not run below quorum; revision advancement prevents replay; host-local emergency authority and no-policy compatibility are explicit; self-issued keys are not human consent, Yjs peers can deny availability, and shared project state cannot attest host policy |
| Drive shared-project title | User explicitly renames/reconciles, retains history, checks recovery, or confirms exact-inventory repair in Workspace/MCP | Captured title tips + title/attribution or recognized active/archive/quarantine inventory → native selected-workspace identity/hash/graph validation → content-addressed event/snapshot → Drive polling/local projection; retention and repair snapshot before any move | Normal title flow includes project/document IDs, title, parent hashes, self-reported attribution/time; recovery automation returns only counts, job timing, and SHA-256 repair/snapshot guards—not titles, authors, filenames, Drive IDs, or bodies | Active metadata events and canonical snapshots; retained valid records in `compacted-title-history/`; invalid active records in recoverable `quarantined-title-history/`; invalid archive/quarantine remains untouched unless a later parent-complete validation incorporates it into a new snapshot; nothing is deleted | Exact 1-20 title guards or exact full-inventory repair hash; stale/concurrent repair moves nothing; maximal parent-closed graph only; 200 rename/400 read/5,000 retained-event/4-MiB snapshot limits; repair inspects 501 active plus 5,200 each archived/quarantined recognized records, 64 snapshots and 32 MiB per location; 200 moves/eight-way, 60-second move/120-second total/30-second request bounds; MCP returns immediately into four jobs with 30-second heartbeats and one-hour terminal retention |
| Drive shared-project catalog | User refreshes selected-workspace projects, opens the cross-workspace browser, or explicitly calls the matching MCP/LAN read | Rust-owned collaboration grant → bounded app-owned root listing → ordered manifest/title metadata reads → product or live automation response | Workspace/project/document identity, parent folder label/code, bounded title state and skipped counts; no OAuth token, Drive file IDs, or document bodies | None; Join separately persists the selected parent workspace | 200 roots/1,000 projects; eight concurrent project-detail reads per serial root; one 12-second cancellation deadline below the 15-second live bridge and derived 20-second MCP socket budgets; malformed/ambiguous/orphan records fail or count as skipped; no mutation authority |
| Local generation | User sends Ask | Webview → loopback llama.cpp | Prompt, selected local/Drive passages, history | Thread in localStorage | Loopback endpoint only |
| Local folder context | User grants folder and asks | Granted path → Rust → model prompt | Relevant text chunks | Knowledge cache in memory | Canonical `Granted` allowlist |
| Local research project | User creates/edits project | Lexical editor ↔ Yjs document ↔ IndexedDB | Rich editor updates and reserved project collections | `syzygy-project-v1:<projectId>` IndexedDB | Schema-versioned manifest; fail-closed migration; no network provider attached |
| Self-hosted WebSocket project | User acknowledges bearer access and creates/accepts a legacy or managed-member invitation; the relay-host operator may issue, rotate/recover, revoke, or install/remove shared-approval policy; a device-bound remote admin may mutate only under the host's current policy | Store-v7 binding + local IndexedDB + Yjs document ↔ configured `y-websocket` relay; bundled mode is a supervised Syzygy child plus a typed host-local membership/policy control plane and a reserved signed remote-admin path | Binary Yjs updates and ephemeral awareness; managed access carries member ID/role/capability/generation/expiry/device key; remote admin carries a semantic action signature and optional exact approval proofs; relay can observe/alter relayed state | Client IndexedDB; bundled per-room document log; digest-only `members-v1.json` with revision/role/status/generation/operator-clock expiry and optional exact signer/quorum policy; awareness memory-only; shared approvals persist through the project's normal Yjs transport; archive strips all live binding/credentials | Private plaintext/public WSS boundary; exact invite/access/action/proof shapes; 256-bit capabilities stored only as SHA-256; 256 rooms/64 members/one active admin; exact revision guards; 1-16 sorted policy signers and bounded quorum; duplicate/foreign/stale/mutated/expired/future/invalid approval denial; restart/remote mutation forces reauthentication; viewers cannot persist writes; absent policy preserves prior managed behavior and absent registry room remains legacy bearer mode; host-local emergency authority remains; no trusted clock, automatic replacement delivery, authenticated person/organization, public TLS, relay-signed approval receipt, or backup claim |
| Signed live-device presence | Live WebSocket awareness first publishes the installation participant ID | Exact project/document/participant/Yjs-client/random-nonce claim → typed Rust command → OS-vault Ed25519 signature → awareness schema v2 → relay peers → strict WebCrypto verification → presence badge | Public key, SHA-256 fingerprint/key ID, claim, signature, display name/focus state; no private key, research body, role, or authorization | Private PKCS#8 key only in OS credential store; public proof is ephemeral awareness and is not written to Yjs, IndexedDB, Drive, archive, relay log, immutable history, or diagnostics | Exact fields; stable-ID/32-byte nonce/u32 client bounds; domain-separated canonical bytes; canonical base64url; public-key/signature sizes; key-ID recomputation; all-field cache keys; 200 in-flight/400 retained verification bounds; cross-language mutation tests; signing failure preserves explicit unsigned collaboration; self-issued key is not trusted enrollment, same-session replay prevention, human identity, role, revocation, or durable-event attribution |
| Signed project device registration | User explicitly clicks **Register this device in project** in a Drive or live project | Exact project/self-reported-participant claim → typed Rust command → same OS-vault Ed25519 key under registration domain → strict WebCrypto verification → distinct Yjs settings record → normal configured project persistence/transport → offline product and MCP inspection | Stable public key/fingerprint, project ID, self-reported participant ID, deterministic signature; no private key, role, approval, relay secret, invitation, or research body | Shared project Yjs/IndexedDB and therefore Drive updates/snapshots, relay document logs, and portable archives; not written to ephemeral awareness or local trust registry | Explicit correlation disclosure; exact storage identity; strict fields/signature/project checks; 200 registration/1,000 setting bounds; eight concurrent verifications; disconnected convergence/offline reopen; conflicting participant claims retained; invalid bodies omitted; deterministic replay/deletion/flood by a bearer peer remains possible; not enrollment, identity, role, shared revocation, or access control |
| Signed durable research-event attribution | Product or MCP commits one exact event in any of all ten adopted domains | Retained exact event → canonical SHA-256 + stable locator/project/participant/time/nonce → typed OS-vault installation signature → unconflicted registered-device check → bounded parallel Yjs attestation → live-event/hash/signature revalidation → content-minimized inspection | Shared record contains public installation key/signature and exact claim metadata/hash; inspection returns key/event/participant/hash/time only and omits public keys, signatures, display names, suggestion/review/scenario/heuristic/rerun bodies, and other research bodies | Shared project settings in Yjs/IndexedDB and normal configured Drive/relay/archive transport; no separate MCP copy | Ten-kind closed native vocabulary; exact resolvers for scenario lifecycle/turn/vote/annotation/label, suggestion, heuristic definition/example/check-result, policy-version, adversarial-review, and scenario-rerun definition/control/item/result events; 2,000 attestation/5,000 setting/eight-verification bounds; exact storage key, live event existence/hash, registered key/public key/participant match, strict signature; signer failure is explicit unsigned after commit and never rolls back the mutation; self-issued installation continuity, not person/organization identity or Sybil resistance |
| Local collaboration device decision | User clicks Approve, Revoke, or Re-approve for an independently verified live presence or durable project-directory fingerprint | Verified proof → exact local project/key/current-status request → typed Tauri command → serialized native registry transition → refreshed local label | Project stable ID, public key fingerprint, expected/next current status, local update timestamp; no private key, research body, participant authority, relay secret, or invitation | Per-installation app-data `collaboration-device-trust-v1.json`; current state only, excluded from Yjs, Drive, archives, relay, invitations, immutable history, and other installations | 1 MiB/64-project/64-device bounds; canonical fingerprint; strict schema; stale-state rejection; one UI mutation at a time; synced temporary replace with primary-first/missing-primary recovery; same-user-editable local preference, not shared enrollment, human identity, role, propagated revocation, or relay authorization |
| Scenario v1 migration | Local reopen, initial Drive pull, or decoded archive persistence encounters valid v1 scenario records | Fully synchronized Y.Doc → strict detached preflight → one Yjs migration transaction → provider publication/fingerprint | Existing scenario/turn/revision content plus deterministic `headEditId`, `parentEditIds`, and `migration-v1` source metadata; no new research body | Same project Y.Doc/IndexedDB and normal configured transport | `migrations.ts` is the only entrypoint; every legacy record validates before the first write; future/hostile/mixed-invalid input is zero-write; deterministic peer migration converges; automation registration follows migration |
| Drive link | User clicks Link/Re-link and consents | System browser ↔ Google; Rust ↔ token endpoint | OAuth code, refresh/access token, account email | `google_auth.json` in app data | PKCE, state, loopback listener; token never returned to webview |
| Drive direct research | Shared mode + Ask | Selected Drive tree → Rust exports → model prompt | Supported relevant text; file labels | No mirror; request memory only | Collaboration scope + persisted folder ID + descendant checks |
| Drive transcript | Shared mode after response | Completed exchange → selected Drive folder | Prompt and model response | Drive file | Explicit per-thread Shared toggle |
| Drive mirror | User clicks Sync | Selected Drive folder ↔ `Documents/Syzygy` | Supported files; Google-native text/CSV snapshots | Local mirror + Drive | Explicit action; exported snapshots are not re-uploaded |
| Drive project compaction | User clicks **Compact Drive history** or explicitly calls revision-guarded MCP maintenance | Final live-provider pull/flush → exact full Yjs snapshot → active Drive updates → same-project `compacted-updates/` archive | Complete project snapshot plus only the content-addressed update file IDs already applied by this provider; product/MCP return counts, not file IDs or bodies | Snapshot remains active; moved records remain recoverable in Drive; local IndexedDB is unchanged | Explicit action; selected-workspace/project/document identity; 4-MiB snapshot; unique applied-ID list; MCP exact document+research revisions checked after final pull; snapshot precedes moves; 200-record/eight-request bound; unknown concurrent and failed moves remain active |
| Model download | User selects model | Publisher URL → app model directory | GGUF/model metadata | App data | Explicit UI action and expected hash/size metadata where available |
| Update check/install | User confirms check/install | GitHub release endpoint → updater | Version metadata, signed installer | Installer/update cache | Explicit disclosure; updater signature |
| Diagnostic log | Runtime errors/milestones | Frontend/Rust error boundary → local ring | Command/tag/error only | localStorage, newest 500 entries | No prompt, file content, token, or credential logging |
| Live MCP read/control | MCP host starts `Syzygy --mcp` and calls a tool | MCP stdio → token-authenticated ephemeral loopback → Rust event → live webview | Semantic method/parameters; project content only for explicit read/write tools | No MCP copy; live Zustand/Yjs owners persist normally | Loopback only, 256-bit per-process bearer, browser-origin rejection, bounded request, timeout |
| MCP research-state inspection | Connected host calls `inspect_research_state` | MCP stdio → authenticated bridge → lifecycle-registered active Y.Doc → signed-device/scenario/vote/annotation/label/heuristic/version/head validators → bounded metadata response | Stable public device fingerprints and self-reported participant IDs plus project/version/scenario/label/heuristic IDs, names/titles, status, attribution, timestamps, counts, integrity issues; no private key or research bodies | None beyond normal Y.Doc/IndexedDB ownership | Read-only route; exact project identity; device signature/conflict/bounds plus scenario record/branch/vote/annotation/label graph and version hash/head/lineage checks; maximum 200 summaries per collection; no registration/approval/identity/role/relay/Drive/model/mutation authority |
| MCP hosted relay-policy administration | Connected same-user host explicitly calls `inspect_relay_approval_policy` or exact-revision `configure_relay_approval_policy` | MCP stdio → authenticated live bridge → active project/Y.Doc identity + running local relay endpoint/room check → fresh native room report + bounded registered-device inspection → optional exact host policy transition | Inspection returns project/room/revision, aggregate member counts, policy key IDs/quorum, and eligible installation key IDs/participant claims; member IDs, capabilities, public keys, storage paths, signatures, and research bodies are omitted. Configure supplies selected key IDs and internally resolves public keys | Policy persists only in host-local relay `members-v1.json`; no MCP copy or shared-project policy record | Host-only exact endpoint/room, healthy directory, 1-16 unique eligible keys, bounded quorum, exact pre-revision and proven +1 response with exact requested policy; disabled policy accepts no hidden signer fields; host emergency authority remains and installation keys do not authenticate people or organizations |
| MCP scenario creation | Connected host explicitly calls `create_scenario` | Exact inspection revision + bounded scenario input → live project/Y.Doc identity check → synchronous scenario-domain transaction | New scenario ID/title/background/status/parent and caller participant ID; response omits no caller-supplied field | Existing project Y.Doc/IndexedDB | Monotonic state-vector revision guard; stale request writes nothing; no turns/model/Drive/UI authority; participant identity unauthenticated |
| MCP scenario index read | Connected host explicitly requests one scenario | Exact active project + validated scenario/revision graph + scenario identity → detached bounded scenario projection | Scenario title/background/status/attribution plus at most 1,000 ordered turn IDs, roles, revision counts, selected heads, complete tip sets, and reconciliation state; no turn bodies | None beyond normal project ownership | Explicit content disclosure; wrong identity/unhealthy graph fails; no mutation/model/Drive authority |
| MCP scenario-turn revision read | Connected host explicitly requests one selected-head, named, or indexed turn revision | Exact active project + validated scenario/revision graph + scenario/turn/edit identity or zero-based revision index → detached single-revision projection | One role/content body, author ID, timestamp, edit ID, parent IDs/source, bounded head/tip metadata, index, and current research revision | None beyond normal project ownership | Explicit content disclosure; selectors are mutually exclusive; at most one 200,000-character revision; wrong identity/unhealthy graph fails; no mutation/model/Drive authority |
| MCP scenario turns | Connected host calls add/revise/reconcile turn | Exact research revision + bounded scenario/turn/role/content/participant; revise derives one exact head; reconcile additionally supplies exact selected head and complete tip set → live identity/graph check → synchronous revision transaction → best-effort exact-revision device attestation → post-attribution revision | Explicit caller-supplied body and attribution; immutable revisions retain parent IDs/source; response returns selected metadata plus signed/unsigned state while attestation inspection omits the body | Existing project Y.Doc/IndexedDB plus parallel bounded settings ledger | Chain returned post-attribution revision; stale requests write nothing; ordinary revision fails with sibling tips; reconciliation appends one all-parent merge without deleting siblings; incomplete tip set is zero-write; cross-author/changed-body claims fail; signing failure never rolls back; no model/Drive/UI authority |
| MCP scenario vote | Connected host calls `cast_scenario_vote` | Exact research revision + scenario/participant/display name/choice → live identity check → immutable vote event → aggregate projection | Caller-supplied attribution and choice enter Y.Doc; response returns counts and totals without voter bodies | Existing project Y.Doc/IndexedDB | Chain returned revision; stale requests add no event; re-votes/withdrawals retain history; participant identity/time are unauthenticated; no model/Drive/UI authority |
| MCP scenario annotation | Connected host creates/edits/resolves/reopens a flag or note | Exact research revision plus annotation current-event guard where applicable → live identity/target checks → immutable parent-linked event → best-effort exact-event installation attestation | Body and caller attribution enter Y.Doc for create/edit; response and inspection omit all bodies/display names and return lifecycle plus signed/unsigned metadata | Existing project Y.Doc/IndexedDB plus parallel attestation in shared settings | Stale project or current-event requests add nothing; resolved notes require reopen before edit; signing failure never rolls back the event; registered installation signature is not human identity; no model/Drive/UI authority |
| MCP scenario label | Connected host creates/renames a label or assigns/removes it from a scenario | Exact research revision plus label/assignment current-event guard where applicable → live identity/target checks → immutable parent-linked event → best-effort exact-event installation attestation → post-attribution revision | Label name and caller attribution enter Y.Doc; response exposes bounded current metadata, signed/unsigned state, and the exact next mutation guard, while attribution inspection omits label names and proof bodies | Existing project Y.Doc/IndexedDB plus parallel attestation in shared settings | Stale project or current-event requests add nothing; first assignment omits a parent and every follow-up requires the exact parent; signing failure never rolls back the event; registered installation is not human identity; no model/Drive/UI authority |
| MCP suggestion lifecycle | Connected host creates a proposal or accepts/rejects a pending proposal | Exact research revision plus exact proposal event for decisions → live project check → immutable suggestion event → best-effort exact-event installation attestation → post-attribution revision | Proposal content, source revision, human/model provenance, or decision attribution enter Y.Doc; response omits proposal/decision bodies and returns stable IDs, provenance category, signed/unsigned state, and the exact next revision | Existing project Y.Doc/IndexedDB plus parallel attestation in shared settings | Stale project/proposal requests add nothing; model proposals require provider/model/run IDs while human proposals prohibit them; decisions never edit the draft; signing failure never rolls back; registered installation is not human identity; no provider/Drive/editor authority |
| MCP immutable checkpoint | Connected host explicitly calls `save_active_policy_version` | Exact live semantic editor snapshot + caller attribution → dual revision checks → canonical hash → immutable version + head | Current policy blocks enter the local version envelope; response returns revision/hash/attribution/count metadata and deterministic count note | Existing project Y.Doc/IndexedDB | Expected document revision checked before hashing and inside final transaction; expected head and parent bytes rechecked in same transaction; no draft edit/restore/Drive/model authority; caller identity is not authenticated |
| MCP setup/self-description | User opens Settings guide or connected host calls `syzygy_installation` | Running Rust process → webview or MCP stdio | Executable path, parent install folder, app/protocol versions, generated config/prompts | None | Local process metadata only; no OAuth token, model secret, or research content |
| Extension contract inspection | Connected host calls `syzygy_platform_contracts` or CI loads validators | Embedded schemas/status → MCP stdio/test process | Provider transports, adversarial phases, plugin permissions/schemas, implementation states | None | Static public contract data; unimplemented runtimes say `contract-only`; no project/key/account data |
| Network boundary self-audit | CI, reviewer, or MCP client loads the S-06 contract | Versioned manifest + production source literals + product-copy anchors → strict headless classifier → sanitized proof | Feature IDs, activation/default state, service origins, route prefixes, payload classes, credential handling, source/copy paths and line numbers | Committed JSON proof only | Unknown fields, missing anchors, duplicate features, or an unclassified origin abort; proof stores origin only and excludes URL credentials, paths, queries, fragments, request bodies, and source text |
| Remote credential management | User expands optional remote keys and saves/replaces/removes | Masked transient DOM field → typed wrapper → Rust → OS credential facility | Provider ID and API key; status returns only a boolean | OS credential store only | Field clears before awaited write; no key in React/Zustand/localStorage/project/backup/log/MCP; no get-secret command; saving has no generation authority |
| Native-gated provider task | Typed workspace single-review call | Structured question/instructions/labeled snapshots/optional safe-subset custom-function schemas → Rust-derived categories/provenance → native disclosure → OS vault → fixed provider HTTPS route → one-shot response or scoped OpenAI/Anthropic/Gemini/xAI event channel → exact schema validation → content-free record | Serialized research envelope and disclosed tool names/descriptions/schemas leave only after native **Send once**; normalized text/events plus transient non-executing tool proposals with valid/invalid/missing-definition, domain-unreviewed, executable-false state, hashes/derived source IDs/status/usage return; private thinking/signature bodies are omitted; xAI's boolean ZDR header is returned as attestation | Key in OS vault; review output/run record remain transient; run record retains no text, tool-argument body, or validation diagnostic, but its output hash commits to all three | No caller approval/category/detached-provenance fields; unique source IDs; depth/node/keyword/byte-bounded schemas; bounded content/model/event/step/tool accumulation and eight diagnostics; denial precedes vault/network; fixed route; xAI attestation precedes event dispatch; timeout/cancellation registry; ordered per-call channel; malformed/incomplete/unsafe-schema calls fail closed; schema success grants no domain or execution authority; no tool result loop or MCP/Drive/filesystem/plugin/editor/network/shared-mutation authority |
| Workspace remote review | Researcher chooses provider/model, edits a question, optionally supplies safe-subset custom-function schemas, and sends once | Current semantic draft + revision → SHA-256 snapshot identity → AJV definition preflight → typed provider task → native disclosure/runtime → incremental OpenAI/Anthropic/Gemini/xAI normalized non-mutating result → AJV streaming display check/native final status | Exact current draft, question, and disclosed tool schemas may leave only after native approval; response text/usage/notices, inspect-only schema-status tool proposals, and xAI ZDR state return to transient UI state | No review persistence; key remains in OS vault; content-free run record is returned | Editable model ID; current-draft binding; explicit one-call UI; scoped ordered channel; fail-closed reducer; cancellation; domain visibly unreviewed and execution false; output and proposals never auto-mutate shared state or execute; no MCP/plugin authority or live-provider proof |
| Provider task proof | Headless fake-network/interoperability test | Memory vault → Rust task bridge → loopback fake provider → serialized record → TypeScript public validators | Synthetic prompt/credential in transport; hashes/source IDs/status/usage in record | Test memory only | Explicit conformance mode, schema/semantic validation, secret/content canaries; native dialog copy tested separately without GUI clicks |
| Adversarial protocol proof | Headless synthetic test invokes the phase runner | Frozen synthetic snapshots → blinded phase runner → strict record + route ledger/baseline | Synthetic questions, excerpts, candidate output, sanitized status/usage | Test memory only | Route outside judge payload; exact equal-call baseline; semantic record gate; pending human decision; shared mutation false |
| Native adversarial execution | Approved product/MCP job executes one planned call | Exact question/sources + frozen graph → random batch capability → atomic reservation → derived phase prompt → OS vault → fixed provider transport → strict JSON/result hash + content-free record | Approved research and exact prior artifacts may leave for the planned provider; normalized validated output returns | Capability/output hashes in Rust process memory for ≤30 minutes; job result in webview memory for ≤1 hour; key in OS vault | Exact content/graph/route/dependency/order/limit/budget binding; one-use calls; upstream SHA-256; malformed/failure consumed; cancellation; fixed endpoint; no hidden reasoning; no automatic shared mutation |
| Collaborative adversarial history | Product user or connected host explicitly shares/saves a completed job or records human judgment | Completed validated transient job + exact research revision + attribution -> canonical SHA-256 archive in peer-namespaced discussions; or exact archive hash/current decision -> immutable accepted/rejected child event | Save makes the full question, selected excerpts, results, baselines, call ledger, and content-free provenance shared project content; decision notes are also shared; routine inspection returns only IDs/hashes/counts/state/integrity | Existing project Y.Doc/IndexedDB and configured collaboration transport, including Drive when bound | 32-MiB/2,000-archive and 20,000-event bounds; exact project/revision/hash/parent guards; full decode/plan/provenance replay validation; exact retry idempotence; concurrent branches fail closed; no editor-root mutation; caller identity/time unauthenticated |
| Product adversarial workflow | Researcher selects exact displayed draft blocks and built-in routes, then approves one native batch | Displayed editor revision + 1–200 selected non-empty blocks + 2–8 perspectives/judge/baseline → credential-presence preflight → shared resumable job → explicit full-content share → shared evidence/history view → exact-parent human decision | Exact selected research leaves only after native approval; completed output stays transient until share copy names question/excerpts/results/baselines/provenance and Drive collaboration consequence | Transient job ≤1 hour unless explicitly archived in project Y.Doc/IndexedDB/collaboration transport | Deterministic `4N + 6` call disclosure; native graph remains authoritative; 30-second heartbeat/15-minute deadline/cancel; history observes discussions only; invalid/conflicted state disables decisions; accept/reject has no editor mutation or Apply control |
| MCP adversarial job | Connected host starts/inspects/cancels | Exact live revision + selected block indexes + built-in routes → immediate job → native batch disclosure/executor → bounded polling | Selected live block text may leave only after native approval; running inspection is metadata-only; completed inspection includes research result | No shared project mutation; terminal job retained ≤1 hour | 8-job ceiling; 30-second heartbeat; 15-minute deadline; exact revision/source derivation; no arbitrary endpoint/prompt/credential/Drive/filesystem authority |
| Plugin authority broker proof | Headless test opens a synthetic host session | Strict manifest + explicit grant + bounded project snapshot → in-process broker → detached snapshot/pending proposal/target decision | Synthetic semantic text, IDs, declared authorities, sanitized error codes | Test memory, maximum 15-minute session | Grant must be manifest subset; exact revision/project/plugin; selected Drive ID; HTTPS host/model allowlists; no loader/fetch/model/Drive/mutation implementation |
| Plugin WIT contract proof | Headless TypeScript/MCP tests inspect public contract | Optional bounded project snapshot → zero-import `run` export → no-change or typed proposals → existing proposal validator | Synthetic project/source text and revision IDs in test memory; WIT source in public MCP metadata | No component executes or persists | No WIT imports; one-MiB envelope ceiling; duplicate-source/unknown-field/cycle/direct-mutation rejection; exact revision guards; truthful no-runtime status |
| Collaborative heuristics | Workspace domain service and future UI/plugins | Validated create/update/delete → nested Yjs heuristic/edit maps → validated sorted projection | Heuristic title/guidance/priority/enabled plus author/time/change values | Existing project Y.Doc and collaboration provider | Stable bounded IDs/text; field-level CRDT merge; peer-specific storage retains colliding edit IDs so projection fails closed; 10,000-edit bound; delete-versus-edit fixture proves no resurrection; no evaluator/model call |
| Explainable heuristic check | Researcher runs the selected heuristic locally or through an API | Exact current semantic policy + selected heuristic + active examples → strict provider-neutral adapter → verified result → revision-guarded immutable Yjs event | Policy, heuristic guidance/examples leave only for an approved remote route; verdict/rationale/uncertainty/cited quotes plus provider/model/run/source and human attribution enter the shared project | Existing namespaced discussions Y.Map | Bounded exact snapshots; no ambient files/tools; native Send once for remote; cancellation; one schema-repair attempt; exact quote/offset validation; project/policy/heuristic/example revisions rechecked before commit; MCP exposes counts/integrity only |
| Versioned scenario rerun | Researcher creates a paused exact-version queue and starts/resumes it | Verified immutable policy version + exact scenario revisions → persistent peer-namespaced queue → sequential strict local/native-approved adapter → atomic result/completion | Local route stays on-device; remote route prepares one exact policy/scenario pair per native Send once; handled/unhandled/uncertain response, rationale, uncertainty, route/model/run, attempt, and attribution enter shared Yjs state | Existing namespaced settings/discussions Y.Maps | 200-item/three-attempt/two-MiB bounds; unique identities; creator-only exact-parent controls; begin before provider; one-item concurrency; 30-second heartbeat; two-minute deadline; pause/cancel/retry; crash resume; completed-item dedupe; stale version/scenario and corrupt/forked state fail closed; MCP exposes counts/integrity only |
| Scenario baseline comparison export | Researcher chooses two completed queues, compares, then explicitly exports | Verified shared queue results + verified immutable policy snapshots → deterministic in-memory comparison → native Save dialog/browser development fallback | Exact policy snapshots, exact scenario revisions, response/rationale/uncertainty bodies, outcome matrix, model/run/attribution, explicit null seed/sampler/model hash, checksums | User-chosen JSON file only; preview remains transient | Same project/document/prompt/scenario IDs/exact revisions; complete-only; 200-row/24-MB bounds; canonical order; policy/revision/artifact SHA-256; all redundant joins and summary recomputed; no network/model call; MCP exposes only compatible-pair count |
| Portable scenario pack | Researcher explicitly exports selected/all scenarios or chooses and confirms an import | Live validated scenario projection → ancestor closure → canonical SHA-256 JSON → native Save; or bounded file → strict decode/checksum/graph/import plan → one Yjs transaction | Scenario titles/backgrounds/lineage, ordered turns, full turn/scenario edit history, author IDs, timestamps, source project/document metadata, optional license; excludes votes, annotations, labels, responses, rerun results, policy/project files, settings, credentials, and transport | User-chosen JSON file for export; existing project scenario Y.Map for confirmed import | 64-MiB/10,000-scenario and nested model bounds; exact fields; canonical histories; closed acyclic graph; exact duplicate skip; any ID/content collision aborts all writes; no model/network call; schema is MCP-discoverable without pack bodies |

| Policy-block edit/reorder | Researcher edits a policy block or uses Move/Alt+Shift+Arrow | Genuine local Lexical edit → stable policy-ID Y.Text content/status plus placement projection; remote Y.Text/root transaction → skip-writeback Lexical projection | Policy text, supported marks/inline embeds, review state, and placement; no model/provider secret | Existing project Y.Doc/IndexedDB and selected Drive workspace when shared | 500,000-code-unit/20,000-segment/embed bounds; exact schema/status/ID; duplicate/malformed fail closed; local atomic backfill; Drive commands require healthy zero-legacy bridge; projection is excluded from Yjs writeback/local undo |
| Editable scenario responses | Researcher writes/edits or explicitly generates/regenerates a selected-scenario variant | Manual textarea or bounded provider result → exact live scenario/response integrity check → immutable exact-parent revision → deterministic shared projection | Response body, scenario/response/revision/parent IDs, installation-provided author/display name/time; model revisions also retain provider/model/run | Versioned peer-namespaced entries in the existing discussions Y.Map and configured collaboration transport | 500,000-character/event-history bounds; manual path performs no model/network call; invalid state blocks before credential/provider work; stale/invalid writes add nothing; disconnected siblings remain retained; product renders 50 responses and 50 recent lineage revisions; identity/time unauthenticated; no policy mutation |
| Editable collaborative scenario turns | Researcher adds, edits, or reconciles a turn in the selected live scenario | Manual role/content draft → live graph validation → exact-current/tip check → synchronous immutable Yjs revision → deterministic shared projection → best-effort exact-revision device attestation | Role/content, scenario/turn/revision/parent IDs, installation-provided author ID and local time; parallel attestation stores only exact hash/proof metadata | Existing project Y.Doc and configured collaboration provider plus bounded settings ledger | 500,000-character, 1,000-turn, and 10,000-revision domain bounds; exact retries idempotent; stale or hostile writes add nothing; disconnected siblings remain retained; retained author/hash required; body omitted from attribution inspection; explicit unsigned fallback after commit; identity/time unauthenticated; no model/network call |
| Collaborative scenario votes | Workspace domain service and future voting UI | Validated vote/re-vote/withdrawal → immutable event in version-prefixed peer bucket → per-participant deterministic projection and aggregate counts | Scenario/event/participant IDs, display-name snapshot, choice, caller timestamp | Namespaced entries in existing discussions Y.Map | Exact replay idempotent; conflicting event ID fails closed; history retained; orphan targets reported; unrelated discussion namespaces ignored; no authentication/model/network call |
| Collaborative scenario annotations | Workspace domain service and selected-scenario flag/note UI | Validated create/edit/resolve/reopen → immutable parent-linked event in separate versioned peer bucket → deterministic lifecycle projection → best-effort registered-installation attestation | Scenario/turn/annotation/event IDs, flag/note body, participant/display-name snapshot, caller timestamp; attestation stores only exact hash/proof metadata | Namespaced entries in existing discussions Y.Map plus parallel bounded settings ledger | Exact-current revision guard; concurrent child branches retained; replay idempotent; collision/orphan checks; body omitted from attribution/MCP; signing failure is explicit unsigned after commit; no human authentication/model call |
| Collaborative scenario labels | Workspace domain service and selected-scenario label UI | Validated label create/rename and scenario add/remove → immutable parent-linked events in separate versioned peer buckets → deterministic projection → best-effort registered-installation attestation | Label/scenario/event/participant IDs, label name, caller timestamp; attestation stores only exact hash/proof metadata | Namespaced entries plus parallel bounded attestation ledger in existing settings Y.Map | Exact-current event guards; concurrent rename branches retained; disconnected assignments merge; collision/orphan checks; label names omitted from attribution inspection; explicit unsigned fallback after commit; no human authentication/model call |
| Immutable policy versions | Workspace domain service and product/MCP save/restore | Structured policy snapshot + lineage/attribution → canonical JSON → SHA-256-keyed Yjs map value → reverified detached projection → best-effort exact-envelope registered-device attestation | Policy blocks, scenario IDs, parent hash, participant ID, display-name-at-save, timestamp, optional note; parallel attestation stores only exact hash/proof metadata | Existing project Y.Doc and collaboration provider, with a bounded parallel settings ledger | Bounded exact schema; canonical newline/reference ordering; valid parent in same project; every read re-canonicalizes and rehashes; retained author must equal attestation participant; no update/delete API; tampered values or cross-author claims fail closed; signing failure leaves checkpoint committed and explicit unsigned; no model/network call |
| Policy version head/restore/diff | Workspace domain service and product/MCP history UI | Exact expected head + current snapshot or verified restore target → atomic version insertion/head pointer → post-commit best-effort attribution; two verified versions → local structured diff | Version hashes, detached policy blocks, attribution status, deterministic change counts | Immutable versions plus one mutable Yjs metadata head and parallel bounded attestation ledger | Stale-head and parent recheck inside one transaction; restore copies into a newly hashed child; concurrent branches survive; returned MCP research revision includes attestation; diff is deterministic/bounded and performs no model/network call |

## Planned flows that are not yet runtime claims

| Flow | Trigger | Source → destination | Required guard |
|---|---|---|---|
| Research plugin | User installs/enables and invokes a contribution | Bounded snapshot → WASI or trusted MCP runtime → typed proposal | declared and granted permissions, no ambient authority, revision guard, diff plus human acceptance |

## Drive boundary nuance

Google's token has wider technical authority than the selected workspace. Syzygy narrows product
operations by folder ID and descendant enumeration. This is an application control, not a Google
permission boundary; see ADR-0001. Any new Drive command must prove that it cannot operate outside
the selected tree or must receive a separate explicit review.

S-06 now has a reproducible source/copy/origin trace in `NETWORK-BOUNDARIES.json` and a sanitized
proof artifact. It is `implemented_unverified`, not `verified`: cross-platform OS packet capture,
dynamic DNS/CDN inspection, third-party SDK payload capture, and paid-provider live calls remain open.

## Evidence still required

- Windows/macOS/Linux app-data permission checks;
- crash-dump inspection for prompt/token leakage;
- two-account Drive harness evidence after restricted-scope reauthorization; and
- packaged Windows/macOS/Linux MCP launch and same-user temp-descriptor permission checks.
